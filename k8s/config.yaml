apiVersion: v1
kind: ConfigMap
metadata:
  name: comspace-config
data:
  api-url: "https://api.comspace.example.com"
---
apiVersion: v1
kind: Secret
metadata:
  name: comspace-secrets
type: Opaque
stringData:
  # IMPORTANT: Replace these with real values before deploying
  # Use `kubectl create secret` or a secrets manager in production
  jwt-secret: "CHANGE_ME_IN_PRODUCTION"
  jwt-refresh-secret: "CHANGE_ME_IN_PRODUCTION"
  mongo-password: "CHANGE_ME_IN_PRODUCTION"
  redis-password: "CHANGE_ME_IN_PRODUCTION"
  stripe-secret-key: "CHANGE_ME_IN_PRODUCTION"
  stripe-webhook-secret: "CHANGE_ME_IN_PRODUCTION"
